# Vercel Deep Root Compatibility Package

This folder supports a stale Vercel command when the Vercel Root Directory is already pointed at the previous compatibility package.

It delegates install/build commands back to the real admin app at `frontend/admin`.
